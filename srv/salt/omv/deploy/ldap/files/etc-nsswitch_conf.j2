{{ pillar['headers']['multiline'] }}

{%- if config.enable | to_bool %}
{%- set nss_module = 'sss' if config.backend == 'sssd' else 'ldap' %}
passwd:         compat {{ nss_module }}
group:          compat {{ nss_module }}
shadow:         compat {{ nss_module }}
{%- else %}
passwd:         compat
group:          compat
shadow:         compat
{%- endif %}

hosts:          files mdns4_minimal [NOTFOUND=return] dns mdns4
networks:       files

protocols:      db files
services:       db files
ethers:         db files
rpc:            db files

netgroup:       nis
